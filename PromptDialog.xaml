<Window x:Class="CustomScreenLocker.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="儲存模板 / Save Preset"
        Icon="app.ico"
        Height="220" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgDark}">

    <Grid Margin="24,20,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtPromptTitle" Text="請輸入模板名稱 (例如: 🏃 運動健身)"
                   Foreground="{StaticResource TextPrimary}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <TextBox Grid.Row="1" x:Name="TxtInput" Style="{StaticResource ModernTextBox}"
                 Height="38" FontSize="14" Margin="0,0,0,18"
                 KeyDown="TxtInput_KeyDown"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="取消" Height="38" Style="{StaticResource ModernButton}"
                    Background="#374151" Click="BtnCancel_Click"/>
            <Button Grid.Column="2" Content="儲存" Height="38" Style="{StaticResource ModernButton}"
                    Click="BtnOk_Click"/>
        </Grid>
    </Grid>
</Window>
